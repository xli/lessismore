# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

one:
  name: Product 1
  user: one
  deployed_revision_url: deployed_revision_url
  deployed_revision_extract_regex: Revision (\w+)
  git_repo_url: git_repo_url
  git_repo_cloned: true

two:
  name: Product 2
  user: one
  deployed_revision_url: deployed_revision_url
  deployed_revision_extract_regex: Revision (\w+)
  git_repo_url: git_repo_url
  git_repo_cloned: false

cloned_no_deploy:
  name: Product 3
  user: one
  deployed_revision_url: deployed_revision_url
  deployed_revision_extract_regex: Revision (\w+)
  git_repo_url: git_repo_url
  git_repo_cloned: true
