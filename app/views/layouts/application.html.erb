<!DOCTYPE html>
<html>
<head>
  <title>Less Is More</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body class="padding-top">

<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="brand" href="/">Less Is More</a>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li class="active"><a href="/">Home</a></li>
          <li><a href="/dashboard">Dashboard</a></li>
          <li><a href="/continuous_deployments">Metrics</a></li>
        </ul>
        <%- if user_signed_in? -%>
        <ul class="nav pull-right">
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= gravatar_image_tag(current_user.email, class: "size24", alt: current_user.email, gravatar: { size: 20 }) %>
              <%= current_user.email %>
            </a>
            <ul class="dropdown-menu" role="menu"
              aria-labelledby="dLabel">
              <li>
                <%= link_to "Change avatar", 'https://en.gravatar.com/', target: 'blank' %>
              </li>
              <li>
              <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
              </li>
            </ul>
          </li>
        </ul>
        <%- else -%>
        <ul class="nav pull-right">
          <li><%= link_to 'Sign in', new_user_session_path %></li>
        </ul>
        <%- end %>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container">
<% if notice %>
  <p class="notice"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert"><%= alert %></p>
<% end %>
<%= yield %>
</div>

</body>
</html>
