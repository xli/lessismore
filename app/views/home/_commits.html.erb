<% if commits.empty? %>
  <div class="zero-commits">Real Artists Ship!</div>
<% else %>
  <ul class="thumbnails">
    <li>
      <div class="commits-size"><%= commits.size %></div>
    </li>
    <%= render(:partial => "commit", :collection => commits) %>
  </ul>
<% end %>
