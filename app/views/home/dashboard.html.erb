<div id="dashboard" class="carousel slide">
  <ol class="carousel-indicators">
    <%= render :partial => "indicator", :collection => ContinuousDeployment.all %>
  </ol>
  <!-- Carousel items -->
  <div class="carousel-inner">
    <%= render(:partial => "continuous_deployment", :collection => ContinuousDeployment.all) || "No Metrics" %>
  </div>
</div>
